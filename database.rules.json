{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "events": {
      ".read": true,
      ".write": "auth != null && !data.exists()",
      ".validate": "newData.hasChildren(['title', 'location', 'startTime', 'endTime'])"
    }
  }
}